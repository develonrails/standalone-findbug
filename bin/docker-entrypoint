#!/bin/bash -e

# Create or migrate existing database on every container start
./bin/rails db:prepare
./bin/rails db:prepare:cache 2>/dev/null || true
./bin/rails db:prepare:queue 2>/dev/null || true

exec "${@}"
